{
  "Logging": {
    "LogLevel": {
      "Default": "Information", // aca limitamos los logs que se muestran en la consola, se mostrara de information para arriba
      "WebApiAuthors.Controllers": "Error", // aca limitamos los logs que se muestran en la consola pero en cierto scope
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "defaultConnection": "Data Source=localhost; Initial Catalog= WebApiCourse; User=SA; Password=Lospibesdela12;"
  },
  "AllowedHosts": "*"
}
